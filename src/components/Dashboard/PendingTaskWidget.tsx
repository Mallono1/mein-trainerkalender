import React from 'react';

const PendingTaskWidget: React.FC = () => {
  return (
    <div className='h-full pb-10 flex items-center justify-center text-slate-400 font-base'>
      No pending tasks!
    </div>
  );
};

export default PendingTaskWidget;
